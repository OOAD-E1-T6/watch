package watch;

import static org.junit.jupiter.api.Assertions.*;

import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;

public class AddNewAlarmTest {
    Alarm alarm = Alarm();

    @Given("User request to go add alarm mode")
    public void moveToAddAlarmMode(String key) {


    }

    @Then("Show new alarm as selected alarm") 
    public void k
    
}
